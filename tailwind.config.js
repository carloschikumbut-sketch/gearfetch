/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./*.html", "./js/**/*.js"], // Scans all HTML in root and JS in the js folder
  theme: {
    extend: {},
  },
  plugins: [],
}